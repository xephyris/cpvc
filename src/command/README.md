# CPVC Command

CPVC command module is for unit testing and verifying the output of `cpvc` functions

## Submodule Status
>[!CAUTION]  
>  The `cpvc::command` module here is primarily used for unit testing and is not for production
>  It is recommended that you use `cpvc`
